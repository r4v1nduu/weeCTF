<%- include('layout') %>

<% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
<% } %>

<main class="container mt-5">
    <section class="mb-5">
        <div class="alert alert-warning mb-4" role="alert">
            <h3>Total Points: <%= user.total_points %></h3>
        </div>
        <h3 class="mb-3">Player Details :</h3>
        <p><strong>Name: </strong> <%= user.full_name %></p>
        <p><strong>URL: </strong> <%= user.ctf_url %></p>
        <h4>Resources:</h4>
        <a href="/downloads/file1.txt">Download File 1</a>
    </section>

    <section class="mb-5">
        <h5>Submit Flags :</h5>
        <form action="/submit-flag" method="post">
            <div class="form-group"></div>
                <input type="text" class="form-control" id="flag" name="flag" required>
            </div>
            <button type="submit" class="btn btn-danger mt-2">Submit</button>
        </form>
    </section>

    <section class="mb-5">
        <h3>Submitted Flags :</h3>
        <ul class="list-group">
            <% flags.forEach(flag => { %>
                <li class="list-group-item"><%= flag.flag %></li>
            <% }); %>
        </ul>
    </section>
</main>
